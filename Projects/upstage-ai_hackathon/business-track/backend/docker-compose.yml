services:
  backend:
    build: .
    container_name: hackathon-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./serviceAccountKey.json:/app/serviceAccountKey.json
    restart: always
